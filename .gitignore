# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.vercel
.env*

# Local working files, not for the repo
groove_opener_pairs.md
opener_validation_results.json
validate_opener_pairs.py
lastfm_spike_results.json
scripts/lastfm_spike.py
replay-out/

# Apple Music badge asset drop (320MB, 97 files: SVG/PNG/EPS × 4 badge
# categories × Badge/Lockup variants). Only one SVG from this set will ever
# ship, copied into public/. The source folder itself must never enter git
# history. Kept even after the folder moves out of the repo, as a guard
# against a future drop landing in the same path.
USGB-EN/

# Working material, not source -- should never end up in history.
Claude outputs/
